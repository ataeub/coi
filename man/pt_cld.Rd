% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pt_cld.R
\name{pt_cld}
\alias{pt_cld}
\title{Create a point cloud object}
\usage{
pt_cld(x, y = NULL, z = NULL)
}
\arguments{
\item{x}{A matrix or data.frame with at least 3 columns (x, y, z), or a
numeric vector of x-coordinates when \code{y} and \code{z} are also provided.}

\item{y}{Numeric vector of y-coordinates. Only used when \code{x} is a vector.}

\item{z}{Numeric vector of z-coordinates. Only used when \code{x} is a vector.}
}
\value{
A \code{pt_cld} object: a 3-column numeric matrix with columns
"x", "y", "z".
}
\description{
Creates a \code{pt_cld} object from a matrix, data.frame, or individual x, y, z
vectors. This is the standard point cloud format used throughout this
package.
}
\examples{
# From vectors
cloud <- pt_cld(1:10, 11:20, 21:30)

# From a data.frame
df <- data.frame(x = 1:10, y = 11:20, z = 21:30)
cloud <- pt_cld(df)

# From a matrix
mat <- matrix(runif(30), ncol = 3)
cloud <- pt_cld(mat)
}
