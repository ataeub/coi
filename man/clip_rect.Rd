% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clip_rect.R
\name{clip_rect}
\alias{clip_rect}
\title{Clip a point cloud to an axis-aligned rectangle}
\usage{
clip_rect(cloud, dim_x, dim_y = dim_x, center = c("center", "origin"))
}
\arguments{
\item{cloud}{A \code{pt_cld} object. Use \code{\link[=as_pt_cld]{as_pt_cld()}} to convert.}

\item{dim_x}{Numeric. Edge length along the x-axis.}

\item{dim_y}{Numeric. Edge length along the y-axis. Defaults to \code{dim_x} (square).}

\item{center}{Either a character string \code{"center"} (geometric center of the
cloud's xy-extent) or \code{"origin"} (coordinates \code{c(0, 0)}), or a
numeric vector of length 2 giving explicit \code{c(x, y)} coordinates.
Partial matching is supported for character inputs.}
}
\value{
A \code{pt_cld} object containing only points within the specified
rectangle.
}
\description{
Clip a point cloud to an axis-aligned rectangle
}
\examples{
cloud <- gen_sphere(20, 0.5)
cloud_clipped <- clip_rect(cloud, dim_x = 5, dim_y = 6, center = "center")
min(cloud_clipped[, "x"])
max(cloud_clipped[, "y"])
}
