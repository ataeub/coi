% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/align_to_north.R
\name{align_to_north}
\alias{align_to_north}
\title{Align point cloud to y axis}
\usage{
align_to_north(cloud, p2, p1 = c(0, 0), heading = "north")
}
\arguments{
\item{cloud}{A \code{pt_cld} object. Use \code{\link[=as_pt_cld]{as_pt_cld()}} to convert.}

\item{p2}{numeric. Length-2 vector giving the target point defining the
heading direction.}

\item{p1}{numeric. Length-2 vector giving the reference point
(default \code{c(0, 0)}).}

\item{heading}{character or numeric. Cardinal direction ("north"/"n",
"east"/"e", "south"/"s", "west"/"w") or a numeric degree offset to add to
the computed alignment angle (default \code{"north"}).}
}
\value{
A \code{pt_cld} object with the rotated coordinates.
}
\description{
Rotates a point cloud in the XY plane so the vector from \code{p1}
to \code{p2} aligns with the y axis, optionally offset by a heading given as a
cardinal direction or custom degrees.
}
\details{
The alignment angle is computed as the angle between the p1->p2
vector and the y axis. If \code{heading} is numeric, it is added as an offset in
degrees. If \code{heading} is a cardinal direction, the corresponding quarter-turn
offset is applied ("north"/"n": 0°, "east"/"e": 360°, "south"/"s": 270°,
"west"/"w": 180°). Rotation is always around the origin (0, 0, 0).
}
\examples{
cloud <- gen_sphere(1, 0.1)
p1 <- c(0, 0)
p2 <- c(1, 1)
aligned <- align_to_north(cloud, p2, p1, heading = "north")
aligned_custom <- align_to_north(cloud, p2, p1, heading = 45)
}
