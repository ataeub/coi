% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clip_z.R
\name{clip_z}
\alias{clip_z}
\title{Clip point cloud vertically}
\usage{
clip_z(cloud, length, from_top = FALSE)
}
\arguments{
\item{cloud}{A \code{pt_cld} object. Use \code{\link[=as_pt_cld]{as_pt_cld()}} to convert.}

\item{length}{(numeric) The length (in Z units) to remove from the cloud.}

\item{from_top}{(logical) If TRUE, remove from top; if FALSE, remove from bottom. Default: FALSE}
}
\value{
A \code{pt_cld} object with the clipped point cloud.
}
\description{
Removes points from the top or bottom of a point cloud along the Z-axis,
preserving the remaining vertical slice.
}
\details{
The function uses the \code{from_top} parameter to determine the clipping direction:
if \code{from_top = TRUE}, points are removed from the top of the cloud (above
max_z - length); if \code{from_top = FALSE}, points are removed from the bottom
(below min_z + length). This is useful for removing canopy or ground points
when focusing on a specific height range.
}
\examples{
cloud <- gen_sphere(50, 1)
# Remove bottom 5 units
clipped_bottom <- clip_z(cloud, 5, from_top = FALSE)
# Remove top 5 units
clipped_top <- clip_z(cloud, 5, from_top = TRUE)

}
