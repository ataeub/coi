% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/point_dist_filter.R
\name{point_dist_filter}
\alias{point_dist_filter}
\title{Filter points by nearest-neighbour distance}
\usage{
point_dist_filter(cloud, max_dist)
}
\arguments{
\item{cloud}{A \code{pt_cld} object. Use \code{\link[=as_pt_cld]{as_pt_cld()}} to convert.}

\item{max_dist}{(numeric) Maximum allowed distance to the nearest
neighbouring point. Must be >= 0.}
}
\value{
A \code{pt_cld} object containing only points with a nearest-neighbour
distance less than or equal to \code{max_dist}.
}
\description{
Removes points whose nearest neighbouring point lies farther away than a
given maximum distance.
}
\details{
For each point in \code{cloud}, the distance to its nearest other point is
computed using a kd-tree via \code{RANN::nn2()}. Points are retained only when
that nearest-neighbour distance is less than or equal to \code{max_dist}.
This can be used to remove isolated points from a point cloud.
}
\examples{
cloud <- rbind(
  gen_sphere(1, 0.5),
  matrix(c(5, 5, 5), ncol = 3, dimnames = list(NULL, c("x", "y", "z")))
)
filtered <- point_dist_filter(cloud, max_dist = 1)
nrow(filtered) == nrow(cloud) - 1

}
