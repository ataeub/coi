% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_dtm.R
\name{extract_dtm}
\alias{extract_dtm}
\title{Extract digital terrain model from point cloud}
\usage{
extract_dtm(
  cloud,
  res,
  sm_type = NULL,
  sm_i = NULL,
  sm_lambda = NULL,
  sm_mu = NULL,
  sm_delta = NULL
)
}
\arguments{
\item{cloud}{A \code{pt_cld} object. Use \code{\link[=as_pt_cld]{as_pt_cld()}} to convert.}

\item{res}{(numeric) Grid cell resolution for DTM extraction. Determines the spacing
of ground points in the resulting mesh.}

\item{sm_type}{(character) Smoothing algorithm type; passed to \code{Rvcg::vcgSmooth()}.
Options include "taubin", "laplace", "fuhrmann", etc. Default: NULL}

\item{sm_i}{(integer) Number of smoothing iterations. Default: NULL}

\item{sm_lambda}{(numeric) Smoothing lambda parameter. Default: NULL}

\item{sm_mu}{(numeric) Smoothing mu parameter. Default: NULL}

\item{sm_delta}{(numeric) Smoothing delta parameter. Default: NULL}
}
\value{
(mesh3d) A 3D mesh object representing the DTM surface, suitable for use
with \code{z_normalize()} or visualization with \code{rgl::shade3d()}.
}
\description{
Extracts a digital terrain model (DTM) mesh from a point cloud by computing
the minimum Z value within each grid cell and triangulating the result.
}
\details{
The function divides the point cloud into a regular grid based on the resolution
parameter, extracts the minimum Z value (ground elevation) for each cell, and
creates a Delaunay triangulation mesh from these ground points. Optional smoothing
can be applied to the resulting mesh using various smoothing algorithms.
}
\examples{
\dontrun{
cloud <- gen_sphere(50, 1)
dtm <- extract_dtm(cloud, res = 2)
# Visualize the DTM
rgl::shade3d(dtm, col = "gray")
}

}
